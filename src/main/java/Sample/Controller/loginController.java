package Sample.Controller;

import javafx.fxml.FXML;
import javafx.scene.control.Button;
import javafx.scene.control.Label;
import javafx.scene.control.PasswordField;
import javafx.scene.control.TextField;

public class loginController {

    @FXML
    private Button loginButton;
    /*
     check to see if login info is correct, if it is, allow access.
     */

    @FXML
    private PasswordField loginPassword;
    /*
    takes user input and checks to see if valid password
     */

    @FXML
    private TextField loginUsername;
    /*
    takes user input and checks to see if valid username
     */

    @FXML
    private Button signUpButton;
    /*
    opens new window that allows user to sign up and created username and password.
     */
    }

